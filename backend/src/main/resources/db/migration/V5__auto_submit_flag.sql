-- =====================================================================
-- VeriProof — 시간 만료 자동 제출 플래그 (백로그 21)
-- =====================================================================
-- 만료 자동 제출(타이머 만료 + 서버 사이드 스위퍼)과 학생 수동 제출을 구분.
-- status는 'SUBMITTED'를 그대로 공유한다 — 자동 제출도 채점/재생/리포트 대상이므로
-- 'EXPIRED'로 분기하면 submittedCount·SESSION_NOT_SUBMITTED 검증이 어긋남.
-- 자동 여부만 별도 컬럼으로 기록해 제출 완료 안내·통계에 사용.
-- =====================================================================

ALTER TABLE exam_session ADD COLUMN auto_submitted BOOLEAN NOT NULL DEFAULT FALSE;
